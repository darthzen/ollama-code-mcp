apiVersion: v1
kind: ConfigMap
metadata:
  name: ollama-code-mcp-config
  labels:
    app.kubernetes.io/name: ollama-code-mcp
data:
  # ollama.ash4d.com resolves to the Ollama LoadBalancer; swap in the
  # cluster-internal service DNS name instead if you ever run Ollama and this
  # server in the same cluster and want to skip the external hop.
  OLLAMA_BASE_URL: "http://ollama.ash4d.com:11434"
  OLLAMA_MODEL: "qwen3-coder:30b"
  OLLAMA_TIMEOUT: "900"
  OLLAMA_NUM_CTX: "8192"
  OLLAMA_MCP_ALLOWED_DIR: "/workspace"
  OLLAMA_MCP_MAX_BATCH_FILES: "20"
  OLLAMA_MCP_DEFAULT_THINK: "true"
  MCP_TRANSPORT: "streamable-http"
  MCP_HOST: "0.0.0.0"
  MCP_PORT: "8765"
